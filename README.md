# How to run the project

```bash
docker-compose up -d --build
```

*train_notebook.ipynb* - model training pipline

Testing the running Triton instance: Run test_initon.ipynb for gRPC[gRPC Remote Procedure Calls] requests to the server.